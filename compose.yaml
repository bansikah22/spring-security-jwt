services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: secure_portal
      POSTGRES_USER: secure_portal
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD:-secure_portal}
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: