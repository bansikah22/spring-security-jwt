spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/secure_portal}
    username: ${DATABASE_USERNAME:secure_portal}
    password: ${DATABASE_PASSWORD:secure_portal}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false